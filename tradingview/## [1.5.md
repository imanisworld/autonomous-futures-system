## [1.5.1] - 2026-05-24

### Added
- Notes section for ORB exit logic (not implemented, just documented)
- Future enhancement: breakeven and trailing stops under consideration

My futures system uses bracket orders (fixed stop and target). I want to add dynamic exit logic:

- After price moves 1x risk in my favor, move stop to breakeven (entry price)
- After price moves 1.5x risk, move stop to lock in 0.5x profit

Do NOT replace bracket orders. Add this as an OPTIONAL overlay that modifies stops after entry.

Implementation: In paper_broker.py, add a method update_stop(current_price) that calculates new stop based on distance from entry. Call this on each new bar while position is open.

Add config flags in risk_rules.yaml:
  enable_breakeven_stop: true
  breakeven_trigger_rr: 1.0
  enable_trailing_stop: false
  trailing_distance_ticks: 10

Write the code changes and tests. Keep existing bracket behavior as default.

What To Track Going Forward
Add to your journal or a separate spreadsheet for each trade:

text
Date:
Time of entry (ET):
Strategy (orb_reclaim / strat_212 / etc):
Win/Loss:
Time of exit (ET):
Notes (e.g., "reclaim at 10:15 AM")
Then after 20 trades, you'll know:

Do most ORB reclaims happen in first hour or later?

Is win rate different by time of day?

Should you add rolling ORB for late session?



cd "/Users/djb.a.e/MAINVSCODE/autonomous-futures-system"
source venv/bin/activate
venv/bin/supervisord -c supervisord.conf
