[build]
builder = "nixpacks"

[deploy]
startCommand = "HOST=0.0.0.0 python -m webhook"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
