# Local-only overrides. Copy to .env.local.
# .env.local must remain ignored by Git.

LOG_DIR=logs
PUBLIC_DEMO_MODE=false

# Use a unique local value; never commit the populated file.
WEBHOOK_SECRET=
